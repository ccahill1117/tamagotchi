import { Loudpack } from './../src/loudpack.js';


  describe ('Loudpack', function() {
  it('should make it work', function() {
    var string = new Sentence ("ok");
    expect(string.string).toEqual("ok");
  })
})
